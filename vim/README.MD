# steps

```bash
cd ${HOME}
curl --create-dirs --output ${HOME}/.vim/autoload/plug.vim --location https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
cp Repository00004/vim/_vimrc ${HOME}/.vimrc
vi ${HOME}/.vimrc
    #:PlugInstall
    #:x
    #:x
```
